---
import type { Locale } from '../i18n/translations';
import { t } from '../i18n/translations';
import ServiceCard from './ServiceCard.svelte';
import { getServices } from '../data/services';
import { getPopups } from '../data/popups';

interface Props {
  lang?: Locale;
}

const { lang = 'cs' } = Astro.props;
const i18n = t(lang);
const services = getServices(lang);
const popups = getPopups(lang);
---

<section class="services">
  <div class="services-inner">
    <div class="services-order">
      <button class="btn btn-primary" data-open-modal="order-form">
        <span class="icon-paper-plane-light"></span>
        {i18n.orderButton}
      </button>
    </div>

    <div class="services-grid">
      {services.map((service) => (
        <ServiceCard
          client:visible
          title={service.title}
          price={service.price}
          popupContent={popups[service.popupId]?.content || ''}
        />
      ))}
    </div>
  </div>
</section>

<style>
  .services {
    padding: var(--section-gap) 15px;
  }

  .services-inner {
    max-width: var(--container-max-width);
    margin: 0 auto;
  }

  .services-order {
    text-align: center;
    margin-bottom: 5em;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }

  @media (max-width: 1024px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 480px) {
    .services-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
