---
import StepCard from './StepCard.svelte';
import { steps } from '../data/steps';
import { popups } from '../data/popups';
---

<section class="hero">
  <div class="hero-inner">
    <h1 class="hero-title">Hledám kvalitní servis na mytí prosklených ploch</h1>
    <p class="hero-subtitle">k cíli je 5 kroků</p>

    <div class="steps-grid">
      {steps.map((step) => (
        <StepCard
          client:visible
          title={step.title}
          subtitle={step.subtitle}
          image={step.image}
          popupContent={popups[step.popupId]?.content || ''}
        />
      ))}
    </div>
    <p class="swipe-hint">posouvejte prstem &nbsp;&#8594;</p>
  </div>
</section>

<style>
  .hero {
    background-image: url('/images/header_bg_new-2.png');
    background-position: bottom center;
    background-repeat: no-repeat;
    background-size: cover;
    padding: 2.5em 15px 10em;
  }

  .hero-inner {
    max-width: var(--container-max-width);
    margin: 0 auto;
  }

  .hero-title {
    text-align: center;
    color: #fff;
    font-size: 45px;
    font-weight: 600;
    margin-bottom: 0;
    line-height: 1.3;
  }

  .hero-subtitle {
    text-align: center;
    color: #fff;
    font-size: 25px;
    line-height: 2em;
    font-weight: 300;
    margin-bottom: 2em;
  }

  .steps-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 1rem;
  }

  .swipe-hint {
    display: none;
  }

  @media (max-width: 1024px) {
    .steps-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 600px) {
    .steps-grid {
      display: flex;
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      -webkit-overflow-scrolling: touch;
      gap: 0.75rem;
      margin: 0 -15px;
      padding: 0 15px;
      scrollbar-width: none;
    }

    .steps-grid::-webkit-scrollbar {
      display: none;
    }

    .hero-title {
      font-size: 1.3em;
    }

    .hero-subtitle {
      font-size: 18px;
      margin-bottom: 1.5em;
    }

    .hero {
      padding-bottom: 6em;
    }

    .swipe-hint {
      display: block;
      text-align: center;
      color: rgba(255, 255, 255, 0.5);
      font-size: 12px;
      font-weight: 300;
      margin-top: 0.8em;
      letter-spacing: 0.05em;
      animation: swipe-fade 3s ease-in-out infinite;
    }

    @keyframes swipe-fade {
      0%, 100% { opacity: 0.4; }
      50% { opacity: 0.8; }
    }
  }
</style>
